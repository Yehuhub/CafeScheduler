DATABASE_URL="file:./dev.db"
SESSION_SECRET="change-me-in-production"
PORT=3000
NODE_ENV=development

# Used by `npm run db:seed` to create the initial boss account.
# Re-running seed only updates the password, not other profile fields.
BOSS_NAME="Boss"
BOSS_USERNAME="boss"
BOSS_PASSWORD="change-me"
